# Hunt Labs — SQL Injection

## Run with Docker

```bash
docker compose up --build
```

Open [http://localhost:5000](http://localhost:5000)

---

## Run without Docker

```bash
pip install -r requirements.txt
python setup_db.py
python -m flask run
```

Open [http://localhost:5000](http://localhost:5000)
